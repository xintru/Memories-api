# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AuthReturnData {
  tokenData: TokenData!
  user: User!
}

type Comment {
  created: DateTime!
  id: String!
  memory: Memory!
  message: String!
  user: User!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Memory {
  comments: [Comment!]!
  created: DateTime!
  description: String!
  id: String!
  name: String!
  user: [User!]!
}

type Mutation {
  addComment(memoryId: String!, message: String!): Comment!
  changePassword(password: String!, token: String!): User!
  createMemory(description: String!, name: String!): Memory!
  forgotPassword(email: String!): Boolean!
  login(email: String!, password: String!): AuthReturnData!
  signup(confirmPassword: String!, email: String!, password: String!): AuthReturnData!
  updateUser(avatar_url: String!, email: String!, name: String!): Boolean!
}

type Query {
  allMemories(limit: Int, page: Int): [Memory!]!
  getMemoryById(id: String!): Memory!
  me: User!
}

type Subscription {
  commentAdded: Comment!
}

type TokenData {
  expiresAt: Float!
  token: String!
}

type User {
  avatar_url: String!
  comments: [Comment!]!
  email: String!
  id: String!
  memories: [Memory!]!
  name: String!
}
